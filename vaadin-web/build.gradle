apply plugin: 'war'

description = 'Vaadin Web Application'
dependencies {
    compile project(':data-entities')
    compile project(':delta-spike-data')
    compile 'org.vaadin:vaadin-java-ee-essentials'
    providedCompile group: 'javax', name: 'javaee-web-api', version: '6.0'
}
